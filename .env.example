# API Backend URL
# In production (Railway), this should be the backend's public domain
# Example: https://engnet-api-production.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:4000

# Node Environment
NODE_ENV=production

# Port (Railway will provide this automatically)
PORT=3000
